export type PackageRecord = {
  name: string;
  version: string;
  description: string;
  author: string;
  repository: string;
  keywords: string[];
  license: string;
  updatedAt: string;
  popularity: number;
};
